import 'package:flutter/material.dart';

class SendLoactionPage extends StatelessWidget {
  const SendLoactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}